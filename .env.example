# Supabase configuration for local development (Vite)
# Copy this file to `.env` and fill in the values.
# NOTE: Vite requires env vars to begin with VITE_ to be exposed to the client.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: If you use a service role key on the server, do NOT expose it to the client.
# Keep service_role keys server-side only.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (DO NOT COMMIT)
